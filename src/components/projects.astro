---
import { Image } from "astro:assets";
import collateral from "../../public/collateral.png";
import paperlane from "../../public/paperlane.png";

interface Project {
  title: string;
  image: ImageMetadata;
  link: string;
  preview: string;
  description: string;
}

const projects: Project[] = [
  {
    title: "Social-Collateral",
    image: collateral as ImageMetadata,
    link: "https://github.com/SocialCollateralAI",
    preview: "https://socialcollateral-landingpage.vercel.app/",
    description: "A social media platform for mental health awareness and support, featuring a clean and modern UI.",
  },
  {
    title: "Paperlane",
    image: paperlane as ImageMetadata,
    link: "https://github.com/aanharits/be_post",
    preview: "https://github.com/aanharits",
    description: "Designed and built an inventory management dashboard for small businesses, focusing on usability and visual clarity.",
  },
  // {
  //   title: "Svgl.app",
  //   image: svgl as ImageMetadata,
  //   link: "https://github.com/pheralb/svgl",
  //   preview: "https://svgl.app",
  //   description: "An open-source library of popular SVG logos. Contributed to the core components and helped improve the search functionality.",
  // },
];
---

<section id="projects" class="py-12 md:py-20 lg:py-28 text-[var(--white)]">
  <div class="max-w-5xl mx-auto px-4">
    
    <!-- Layout like About Section - Title left, Projects right -->
    <div class="grid grid-cols-1 md:grid-cols-12 gap-8 md:gap-12">
      
      <!-- Title Column -->
      <div class="md:col-span-3">
        <h2 class="text-2xl md:text-3xl font-medium italic text-[var(--sec)] mb-4">
          Projects
        </h2>
        <a
          target="_blank"
          href="https://github.com/aanharits"
          class="inline-flex items-center gap-2 text-sm text-[var(--white-icon)] hover:text-[var(--sec)] transition-colors group"
        >
          more
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="w-4 h-4 group-hover:translate-x-1 transition-transform">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
      </div>

      <!-- Projects Carousel -->
      <div class="md:col-span-9">
        <div
          id="projects-carousel"
          class="projects-carousel flex gap-6 overflow-x-auto scroll-smooth pb-4 cursor-grab active:cursor-grabbing"
        >
          {
            projects.map((project, index) => (
              <a
                href={project.preview}
                target="_blank"
                rel="noopener noreferrer"
                class="project-card group block min-w-[280px] flex-[0_0_calc(33.333%-1rem)] lg:flex-[0_0_calc(33.333%-1rem)] md:flex-[0_0_calc(50%-0.75rem)] max-sm:flex-[0_0_85%] snap-start select-none no-underline"
              >
                <div class="border border-[var(--white-icon-tr)] rounded-[14px] overflow-hidden transition-all duration-300 h-full flex flex-col group-hover:border-[var(--sec)] group-hover:shadow-[0_8px_32px_rgba(0,0,0,0.08)]">
                  <!-- Project Image -->
                  <div class="overflow-hidden aspect-[16/10]">
                    <Image
                      src={project.image}
                      alt={project.title}
                      width={400}
                      format="webp"
                      quality={75}
                      loading="lazy"
                      decoding="async"
                      class="w-full h-full object-cover transition-transform duration-[600ms] ease-[cubic-bezier(0.4,0,0.2,1)] group-hover:scale-105 pointer-events-none"
                    />
                  </div>

                  <!-- Card Content -->
                  <div class="p-5 pb-6 flex-1 flex flex-col">
                    <div class="flex items-center justify-between mb-2.5">
                      <span class="text-[0.7rem] font-semibold tracking-widest text-[var(--sec)] opacity-70">
                        {String(index + 1).padStart(2, "0")}
                      </span>
                      <svg
                        class="text-[var(--white-icon)] opacity-0 -translate-x-1 translate-y-1 transition-all duration-300 group-hover:opacity-100 group-hover:translate-x-0 group-hover:translate-y-0 group-hover:text-[var(--sec)]"
                        xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
                      >
                        <path d="M7 17L17 7M17 7H7M17 7V17" />
                      </svg>
                    </div>
                    <h4 class="text-[1.05rem] font-semibold text-[var(--white)] mb-2 transition-colors duration-300 group-hover:text-[var(--sec)]">
                      {project.title}
                    </h4>
                    <p class="text-[0.82rem] leading-relaxed text-[var(--white-icon)] flex-1">
                      {project.description}
                    </p>
                  </div>
                </div>
              </a>
            ))
          }
        </div>

        <!-- Dot Indicators -->
        <div class="flex justify-center gap-2 mt-5" id="carousel-dots">
          {projects.map((_, i) => (
            <button
              class={`carousel-dot h-2 rounded-full border-none bg-[var(--white-icon-tr)] cursor-pointer transition-all duration-300 p-0 hover:bg-[var(--white-icon)] ${i === 0 ? "w-5 !bg-[var(--sec)]" : "w-2"}`}
              data-index={i}
              aria-label={`Go to project ${i + 1}`}
            />
          ))}
        </div>
      </div>

    </div>

  </div>
</section>

<style>
  /* Only styles that have no Tailwind equivalent */
  .projects-carousel {
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    -ms-overflow-style: none;
  }
  .projects-carousel::-webkit-scrollbar {
    display: none;
  }
  .project-card {
    -webkit-user-drag: none;
  }
  .project-card img {
    pointer-events: none;
  }
</style>

<script>
  const carousel = document.getElementById("projects-carousel");
  const dots = document.querySelectorAll(".carousel-dot");

  if (carousel && dots.length > 0) {
    // Update active dot on scroll
    carousel.addEventListener("scroll", () => {
      const scrollLeft = carousel.scrollLeft;
      const cardWidth = carousel.querySelector(".project-card")?.clientWidth || 280;
      const gap = 24; // 1.5rem
      const activeIndex = Math.round(scrollLeft / (cardWidth + gap));

      dots.forEach((dot, i) => {
        if (i === activeIndex) {
          dot.classList.add("w-5", "!bg-[var(--sec)]");
          dot.classList.remove("w-2");
        } else {
          dot.classList.remove("w-5", "!bg-[var(--sec)]");
          dot.classList.add("w-2");
        }
      });
    }, { passive: true });

    // Click dot to scroll
    dots.forEach((dot) => {
      dot.addEventListener("click", () => {
        const index = Number(dot.getAttribute("data-index"));
        const cardWidth = carousel.querySelector(".project-card")?.clientWidth || 280;
        const gap = 24;
        carousel.scrollTo({ left: index * (cardWidth + gap), behavior: "smooth" });
      });
    });

    // Drag to scroll
    let isDown = false;
    let startX: number;
    let scrollLeft: number;

    carousel.addEventListener("mousedown", (e) => {
      isDown = true;
      startX = e.pageX - carousel.offsetLeft;
      scrollLeft = carousel.scrollLeft;
    });

    carousel.addEventListener("mouseleave", () => { isDown = false; });
    carousel.addEventListener("mouseup", () => { isDown = false; });

    carousel.addEventListener("mousemove", (e) => {
      if (!isDown) return;
      e.preventDefault();
      const x = e.pageX - carousel.offsetLeft;
      const walk = (x - startX) * 1.5;
      carousel.scrollLeft = scrollLeft - walk;
    });
  }
</script>
